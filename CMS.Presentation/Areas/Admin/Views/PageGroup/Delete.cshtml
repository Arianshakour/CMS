@using CMS.Domain.Dtoes.PageGroup
@model DeletePageGroupDto

<div class="row">
    <div class="col-md-4;input-group" style="font-family : yekan">
        <form method="post" style="text-align:center" id="deletePageGroup">
            <h2>آیا از حذف اطمینان دارید ؟</h2>
            <input type="hidden" asp-for="GroupId" />
            <div class="input-group2">
                <label asp-for="@Model.GroupTitle" class="control-label"></label>
                <input asp-for="@Model.GroupTitle" class="form-control" readonly />
            </div>
            <div class="input-group2 align-items-center" style="margin-top:8px">
                @* <button type="submit" class="btn btn-success" asp-controller="PageGroup" asp-action="Delete" style="width:450px;margin-top:8px">حذف</button> *@
                <button type="button" class="btn btn-success" onclick="DeletePageGroup()" style="width:450px;margin-top:8px">حذف</button>
                <a type="button" class="btn btn-secondary" data-bs-dismiss="modal" data-bs-toggle="modal" data-bs-target="#mymodal" style="width:450px;">بازگشت</a>
            </div>
        </form>
    </div>
</div>