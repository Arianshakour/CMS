@using CMS.Domain.Dtoes.PageGroup
@model EditPageGroupDto

<div class="row">
    <div class="col-md-8;">
        <form method="post" style="text-align:center" id="editPageGroup">
            <h2>ویرایش دسته بندی</h2>
            <input type="hidden" asp-for="GroupId" />
            <div class="mb-3">
                <label asp-for="@Model.GroupTitle" class="control-label"></label>
                <input asp-for="@Model.GroupTitle" class="form-control" />
                <span asp-validation-for="@Model.GroupTitle" class="text-danger"></span>
            </div>
            <div class="d-flex justify-content-around gap-3">
                @* <button type="submit" class="btn btn-success" asp-controller="PageGroup" asp-action="Edit" style="width:450px;margin-top:8px">ثبت</button> *@
                <button type="button" class="btn btn-success" onclick="EditPageGroup()" style="width:450px;">ثبت</button>
                <a type="button" class="btn btn-secondary" data-bs-dismiss="modal" data-bs-toggle="modal" data-bs-target="#mymodal" style="width:450px;">بازگشت</a>
            </div>
        </form>
    </div>
</div>